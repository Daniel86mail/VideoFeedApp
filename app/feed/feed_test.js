'use strict';

describe('myApp.feed module', function() {

  beforeEach(module('myApp.feed'));

  describe('feed controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var feedCtrl = $controller('feedCtrl');
      expect(feedCtrl).toBeDefined();
    }));

  });
});